# Always color, even if piping to a another program
--color

# Use "less −r" as my pager
--pager
less -r

--ignore-dir=node_modules
--ignore-dir=env
# Uber-specific ignores
--ignore-dir=src/shared/page-modules
--ignore-dir=page-components
--ignore-dir=dist-client
--ignore-dir=dist-server
--ignore-dir=dist-shared
--ignore-dir=dist-test
